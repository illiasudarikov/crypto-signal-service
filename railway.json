{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacks": {
      "buildCommand": "npm install"
    }
  },
  "deploy": {
    "startCommand": "node server.js",
    "regions": ["us-east1"]
  }
}
